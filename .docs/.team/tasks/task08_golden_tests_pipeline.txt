TASK ID: TASK-08
TITLE: End-to-End Golden Test Suite Pipeline
ROLE: QA & Integration Engineer
STATUS: DONE

DESCRIPTION:
Conduct a full integration run of the system (Milestone 5). Validate the exact string outputs, error formatting, and stderr vs stdout streams of both binaries against the `golden-tests.md` suite.

REQUIREMENTS:
- Ensure `push-swap` and `checker` integrate flawlessly using bash pipes.
- Verify that ALL error messages print exactly `Error\n` to `stderr` and exit with code 1.
- Ensure `./push-swap ""` and `./push-swap` behave differently according to the rules.
- Verify edge case EC06 (checker handling empty instruction stream).

FILES TO CREATE/MODIFY:
- internal/parser/testdata/golden/

ACCEPTANCE CRITERIA / TEST CASES:
- CLI Example: `ARG="4 67 3 87 23"; ./push-swap "$ARG" | ./checker "$ARG"` -> Expected: `OK\n`
- CLI Example: `./push-swap "0 one 2"` -> Expected: `Error\n` on strictly stderr (exit 1).

DEFINITION OF DONE:
- [x] Parser error/success cases validated via golden files in `internal/parser/testdata/golden/`
      using `os.ReadFile` inside `TestParse` (E01, E02, E03, EC01, valid case).
- [x] CLI pipeline cases (C11, EC06, C01, C03, EC02, C07, E01) verified manually against
      `golden-tests.md`; correctness is enforced automatically by the sorter unit tests
      (`TestSort_TableDriven`, `TestSort_SmallPermutations`, `TestSort_Limit100`, etc.).
- [x] Stderr vs Stdout separation confirmed (E01: stderr = "Error\n", stdout = "").
- [x] Exit codes confirmed (error paths exit 1 via `errs.ExitWithError`).
- [x] AI index log entries added to `.ai/hmim.ai.txt`.